const Header = () => {
  const estiloPersonalizado = {
    height: "5vh",
  };
  return (
    <header style={estiloPersonalizado} className=" w-100 bg-primary">
      Header
    </header>
  );
};

export default Header;
